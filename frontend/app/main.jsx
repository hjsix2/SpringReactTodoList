var MainComponent = React.createClass({
    render: function () {
        return (
            <div>
                React works!
            </div>
        )
    }
});

ReactDOM.render(<MainComponent/>, document.getElementById('react-target'));